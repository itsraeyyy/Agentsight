{
  "name": "agentsight",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:mcp": "npm run dev --workspace=@itsraeyy/agentsight-mcp",
    "build": "npm run build --workspaces"
  }
}
